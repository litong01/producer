[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
# Run only unit tests by default; use -m integration for 童年.pdf pipeline test
addopts = -v
markers =
    integration: run full pipeline with 童年.pdf (requires Audiveris, e.g. in Docker)
